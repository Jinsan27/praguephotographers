$(document).ready(function(){
  $(".owl-carousel").owlCarousel();
});

$('.owl-carousel').owlCarousel({
    items: 1,
    autoHeight: true,
    nav: true
});

$('.owl-carousel').owlCarousel({
    loop: true,
    autoWidth: true,
    autoHeight: true,
    items: 1,
    autoplay: true,
    autoplayTimeout: 1000,
    autoplayHoverPause: true,
    autoplaySpeed: true
});